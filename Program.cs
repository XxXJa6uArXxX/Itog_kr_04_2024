using System;

class Prog
{

Console.WriteLine("Введите количество строк в массиве:");
int N = Convert.ToInt32(Console.ReadLine());

for (int i = 0, i < N, int++)

 {
            Console.WriteLine("Введите элементы для массива №{i}:");
            initialArray[i] = Console.ReadLine();
 }

string[] arr = new string[N];

}